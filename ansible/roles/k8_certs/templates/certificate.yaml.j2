apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: infiquetra-certificate
  namespace: {{ cert_manager_namespace }}
spec:
  secretName: {{ secret_name }}
  issuerRef:
    name: {{ issuer_name }}
    kind: ClusterIssuer
  dnsNames:
    - "*.infiquetra.com"
    - infiquetra.com
