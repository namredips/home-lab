---
- name: Run proxmox_cluster setup
  ansible.builtin.import_tasks: setup.yml
  when: not reset | bool
  tags: proxmox_cluster

- name: Run proxmox_cluster reset
  ansible.builtin.import_tasks: reset.yml
  when: reset | bool
  tags: proxmox_cluster
