---
- name: Run proxmox_vm setup
  ansible.builtin.include_tasks: setup.yml
  when: not reset | bool
  tags: proxmox_vm

- name: Run proxmox_vm reset
  ansible.builtin.include_tasks: reset.yml
  when: reset | bool
  tags: proxmox_vm
