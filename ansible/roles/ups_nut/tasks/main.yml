---
- name: NUT UPS setup
  ansible.builtin.include_tasks: setup.yml
  when: not reset | bool
  tags: ups_nut

- name: NUT UPS reset
  ansible.builtin.include_tasks: reset.yml
  when: reset | bool
  tags: ups_nut
