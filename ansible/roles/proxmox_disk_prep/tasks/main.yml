---
- name: Disk wipe for Ceph OSD preparation
  ansible.builtin.include_tasks: setup.yml
  when: not reset | bool

- name: Disk prep reset (no-op)
  ansible.builtin.include_tasks: reset.yml
  when: reset | bool
