---
# Deploy Dell fan speed control to all Proxmox hosts
# Sets a static minimum fan floor (default: 20% PWM). NOTE: this disables iDRAC
# automatic thermal ramping — fans are locked to the floor value until reset.
# Hosts with fan_control_enabled: false in host_vars are automatically skipped (e.g. r720xd).
#
# Usage:
#   ansible-playbook -i inventory/hosts.yml fan_control.yml \
#     --vault-password-file ~/.vault_pass.txt
#
# Reset (restore automatic fan control):
#   ansible-playbook -i inventory/hosts.yml fan_control.yml \
#     --vault-password-file ~/.vault_pass.txt -e reset=true

- name: Configure Dell fan speed control
  hosts: proxmox_hosts
  become: true
  roles:
    - role: proxmox_fan_control
